# PowerNet — протокол розвозчика сіна JH Agro

Контекст проєкту для роботи в терміналі. Усе нижче відновлено статичним аналізом
`JHAgroPCPanelApp_exe_v2_2_294.exe` (реверс-інжиніринг заводського застосунку).
Прошивку плати не аналізували, живий трафік не знімали.

---

## 1. Про машину

Рейковий кормороздавач (візок) JH Agro / SKIOLD. Їздить по монорейці по корівнику,
розвозить корм. Керується з ПК по COM-порту.

Складові:
- **Візок** — плата керування, кілька моторів, датчики, вагова система
- **Стрілки на рейках** — окремі радіовузли (коробочки з антеною), кожен зі своїм
  сервоприводом, перемикають напрямок колії
- **ПК-панель** — заводський застосунок `JHAgroPanelApp.exe`

**Мета проєкту:** написати власну панель керування (обрано JavaScript / Node.js).

---

## 2. Походження даних

`.exe` виявився **інсталятором Inno Setup 5.5.7**, а не застосунком.

```bash
innoextract JHAgroPCPanelApp_exe_v2_2_294.exe    # -> app/
```

Усередині — **.NET / WPF** застосунок:

| Файл | Роль |
|---|---|
| `JHAgroPanelApp.exe` | UI, маршрути, стан-машина виконання |
| `PowerNetDriver.dll` | **весь протокол зв'язку із залізом** |
| `DataLogger.dll` | логування |
| `Xceed.Wpf.*` | сторонній UI-фреймворк, не цікавий |

`PowerNetDriver.dll` — AnyCPU, .NET Framework 4.0, залежить лише від `mscorlib`,
`System`, `System.Core`. Не обфускований.

Дизасемблювання в IL:

```bash
ikdasm app/PowerNetDriver.dll > PowerNetDriver.il
ikdasm app/JHAgroPanelApp.exe  > JHAgroPanelApp.il
```

Для чистого C# краще **ILSpy / dnSpy** на Windows — DLL декомпілюється майже
в оригінальний код.

> **Порада:** якщо треба уточнити деталь протоколу — шукати в `PowerNetDriver.il`.
> Якщо логіку маршруту, станів, стрілок — у `JHAgroPanelApp.il`.

---

## 3. Фізичний рівень

| Параметр | Значення |
|---|---|
| Інтерфейс | RS232 (DB9, живий кабель до плати — перевірено фізично) |
| Швидкість | **115200** — підтверджено на живій платі (`scan.js`, COM1 @ 115200 → валідна відповідь на keepalive) |
| Формат | **8N1** (8 біт даних, без парності, 1 стоп-біт) |
| Таймаут відповіді | 200 мс (`0xC8` у коді) |
| Повторів | 5 (`repeat = 5` у станах) |

> **Історія питання зі швидкістю:** в IL `PowerNetDriver.dll` є два конфліктуючих
> дефолти — `SerialPortWrapper` (порожній конструктор) виставляє 9600, а
> `PortsManager.CheckAndAddIfNeed` (сканер портів) явно перевизначає на 115200.
> Це збило з пантелику: Device Manager і "Port Settings" показували 9600, тож
> спершу здавалось, що саме це правильне значення. Насправді "тиша" на 115200
> траплялась через **заводський застосунок, що тримав порт одночасно** (два
> майстри на лінії, див. §11) — щойно порт звільнили, 115200 одразу дав
> відповідь. Device Manager показує дефолтні властивості порту, не те, що
> застосунок реально виставляє при відкритті — тут це виявилось нерелевантним.

---

## 4. Формат кадру

Довжина до стаффінгу = `len(data) + 8`.

```
[0]        0x7E                              стартовий прапорець
[1]        (local << 3) | (remote >> 2)      адреси, бітова упаковка
[2]        ((remote << 6) & 0xFF) | register  регістр = молодші 6 біт
[3]        (msgType << 6) | (0x20 якщо запис)
[4]        len(data)
[5..]      data
[-3]       CRC low                            LSB перший
[-2]       CRC high
[-1]       0x7E                               стоповий прапорець
```

Адреси за замовчуванням: **local = 0, remote = 1**.

`msgType`: `0 = Request`, `1 = Reply`, `3 = Broadcast`
`ReadWriteType`: `0 = Read`, `1 = Write` (біт `0x20` у байті 3)

### CRC-16

Стандартний **PPP FCS-16**: поліном `0x8408` (reflected), ініціалізація `0xFFFF`,
**без** фінального інвертування. Рахується по байтах `[1 .. len-4]` — без
стартового прапорця, без CRC і без стопового.

> Таблицю з DLL звірено з генерованою — **збіг 256 із 256 записів**.

```python
crc = 0xFFFF
for b in buf[1:-3]:
    crc = (crc >> 8) ^ TABLE[(crc ^ b) & 0xFF]
```

### Байт-стаффінг

Застосовується до всього **між прапорцями** (включно з CRC):

```
0x7E  ->  0x7D 0x5E
0x7D  ->  0x7D 0x5D
```

---

## 5. Повна мапа регістрів

| Рег | Метод драйвера | Напр. | Дані | Опис |
|---:|---|---|---:|---|
| 1 | `KeepAlive` | R | 2 | підтримка зв'язку + напруга АКБ (big-endian мілівольти, перевірено на живій платі) |
| 2 | `ForceToBootloaderMode` | W | — | у бутлоадер (**не чіпати**) |
| 3 | `ReedSWHVVersion` | R | — | версії SW / HW |
| 4 | `ReqBoardError` | R | — | маски помилок |
| 5 | `ResetErrorMask` / `GetMotorStatus` | W/R | 1 | скид помилок / статус мотора |
| 6 | `GetMotorStatus` | R | 1 | статус мотора за ID |
| 7 | **`SetMotorSettings`** | W | 9 | **керування мотором** |
| 8 | `SetIRState` | W | 3 | ІЧ-канал (enable + час) |
| 9 | `SetAlarmState` | W | 1 | сирена |
| 10 | `SetImpulseOutState` | W | 2 | два імпульсні виходи |
| 11 | `GetInductiveOpticalSensorState` | R | — | **датчики положення** |
| 12 | `GetFoodWeightValue` | R | — | вага корму |
| 13 | `SetActuatorsState` | W | 2 | два актуатори (на візку) |
| 14 | `GetAIState` | R | — | аналогові входи |
| 15 | `GetDIState` | R | — | **кнопки, E-Stop** |
| 16 | `SetChargeState` | W | 1 | реле заряду |
| 17 | `SetMotorState` | W | 1 | силове реле моторів |
| 18 | `SetSpareOut1` | W | 1 | запасний вихід |
| 19 | **`SetWirelessState`** | W | N+1 | **таблиця стрілок + команда** |
| 20 | `ResetSwitchZone` | W | 3 | скид зони стрілок |
| 21 | `Set/GetSwitchAmount` | W/R | 2 | кількість стрілок |
| 22 | `GetWirelessACK` | R | — | **підтвердження від вузла** |
| 23 | `GetCurrentSwitchZoneState` | R | — | стан зони |
| 25 | `SetMotorRequirementsMask` | W | 2 | маска задіяних моторів |
| 26 | `GetWirelessState` | R | — | стан радіомодуля |
| 28 | **`SetWirelessCommand`** | W | 2 | **пряма команда вузлу** |
| 29 | `GetEventWirelessCommand` | R | — | подія від радіо |
| 254 | — | — | — | помилка COM-порту (внутрішня) |

---

## 6. Керування моторами (регістр 7)

Дані — 9 байт:

| Байт | Поле | Примітка |
|---:|---|---|
| 0 | `MotorID` | номер мотора |
| 1 | `ev` | у тест-панелі дорівнює `Status` |
| 2 | **`Status`** | **1 = рух, 0 = стоп** |
| 3 | `Power` | обмеження потужності/струму |
| 4 | `PowerDelay` | затримка спрацювання захисту |
| 5 | **`Speed`** | 0–255 |
| 6 | `RampUp` | розгін |
| 7 | `RampDown` | гальмування |
| 8 | **`Direction`** | напрямок |

> **Реальна формула Speed** (з `ManualControlViewModel.DoForward/DoBackward`,
> перевірено на живій платі): для кнопок Forward/Backward у заводській панелі
> `Speed` НЕ дорівнює проценту зі settings напряму:
> ```
> Speed = ((MotorDriveSpeed - 32) * CartSpeedPercent + 3200) / 100
> ```
> де `CartSpeedPercent` — `CartSpeedLow`/`CartSpeedNormal` (40%/70% на типовій
> машині), `MotorDriveSpeed` — калібрувальний байт з `TechnicalMotorSettings`.
> Для типових значень це дає **`Speed` ≈ 120–190**, а не 10–20. На практиці
> `Speed=15` **не зрушував** Drive1+Drive2 (плата приймала команду, реле
> спрацьовувало, мотор не крутився) — схоже, це нижче порогу зрушення під
> навантаженням; `Speed≈100` і вище — рухає.

> **Реальні калібрувальні числа цієї машини** — з живого
> `Settings.xml` (`%APPDATA%\JHAgroPanelApp\Settings\Settings.xml`, звірено з
> масивом `TechnicalSettings.Motors[]` в IL). Індекси масиву відповідають
> `Motor id` в XML (`id=N` → `Motors[N-1]`), порядок підтверджено з побудови
> маски регістру 25 (`ManualControlViewModel`, кожен індекс OR-иться зі своїм
> бітом `MOTOR_IDS`):
>
> | idx (XML id) | Мотор | MotorID біт | MaxPower | PowerDelay | RampUp | RampDown | MotorDriveSpeed | DirectionMotorPlate | MotorMaskDrive |
> |---:|---|---:|---:|---:|---:|---:|---:|:---:|:---:|
> | 0 (1) | Drive1 | 1 | 44 | 110 | 10 | 0 | 255 | true | **true** |
> | 1 (2) | Drive2 | 2 | 44 | 110 | 10 | 0 | 255 | true | **true** |
> | 2 (3) | Extra | 4 | 15 | 110 | 0 | 0 | 255 | false | false |
> | 3 (4) | Conveyor | 16 | 36 | 110 | 10 | 0 | 255 | true | **true** |
> | 4 (5) | Shredder | 8 | 44 | 110 | 10 | 0 | 255 | true | **true** |
> | 5 (6) | RightPlate | 32 | 15 | 110 | 0 | 0 | 32 | false | false |
> | 6 (7) | LeftPlate | 64 | 15 | 110 | 0 | 0 | 32 | false | false |
>
> `CartSpeedLow=40`, `CartSpeedNormal=70` на цій машині. Для Drive1/Drive2
> (`MotorDriveSpeed=255`): `Speed=121` при `CartSpeedLow` (авто-рух, стан
> `StateBackwardMoving` логує саме `"Rail is near, DriveBackward (CartSpeedLow)"`
> біля стрілок), `Speed=188` при `CartSpeedNormal` (ручні кнопки
> Forward/Backward/BackwardToHome в `ManualControlViewModel`).
>
> **Напрямок підтверджено з IL** (`DoForward`/`DoBackward`/`DoBackwardToHome`,
> усі три узгоджені): якщо `DirectionMotorPlate`(Drive1)==true — `Forward`
> шле `Direction=0`, `Backward`/`BackwardToHome` шлють `Direction=1`. На цій
> машині `DirectionMotorPlate=true`, тож **`Direction=1` — це справді "назад"**,
> як і стояло в `go_home.js` за замовчуванням (раніше — припущення "звірити
> на місці", тепер підтверджено з коду).
>
> **`SetMotorRequirementsMask` (рег.25) для цієї машини = `27` (`0x1B`)** —
> `Drive1 | Drive2 | Conveyor | Shredder`. Extra/RightPlate/LeftPlate
> вимкнені в `Settings.xml` (`MotorMaskDrive=false`) — вони фізично не
> встановлені або не каліброван на цій машині. `go_home.js` і `server.js`
> тепер шлють саме цю маску, а не "усі 7" — ширша маска могла провокувати
> `MotorLostCommunMask` на моторах, яких плата не очікує задіяними.
>
> Для комбінованої команди `MotorID=3` (`Drive1|Drive2`) `DoForward/DoBackward/
> DoBackwardToHome` беруть `Power/PowerDelay/RampUp/RampDown/Direction` **лише
> з `Motors[0]` (Drive1)** — на цій машині це неважливо, бо `Motors[1]`
> (Drive2) має ідентичні значення, але загалом означає: калібрування Drive2
> в `Settings.xml` для парної команди ігнорується.

### Мотори в системі

З `DataExchangeTestViewModel` (вбудована тестова панель) видно набір приводів:

```
Drive1, Drive2, Extra, Conveyor, Shredder, RightPlate, LeftPlate
```

`MotorID` підтверджено з IL (`Drive.ctor()`, `DischargeFeed.ctor()`,
`Motor.SendMotorSettings()`) — бітова маска, не послідовні номери:
`Drive1=1, Drive2=2, Extra=4, Shredder=8, Conveyor=16, RightPlate=32, LeftPlate=64`.

> Джерело реальних калібрувальних чисел (Power/PowerDelay/RampUp/RampDown/
> MotorDriveSpeed/DirectionMotorPlate/MotorMaskDrive за кожним мотором) —
> `Settings.xml` заводського застосунку: `%APPDATA%\JHAgroPanelApp\Settings\
> Settings.xml` на машині, де стоїть `JHAgroPanelApp.exe` (звичайний текстовий
> XML). Якщо машину перекалібрують або зміниться конфігурація моторів —
> звірити цей файл заново, таблиця в §6 нижче відповідає знімку станом на
> 2026-07-25.

> **Важливо для Drive1+Drive2:** реальний шлях руху візка (`Cart.DriveForward/
> DriveBackward/DriveStop` → `Drive.Forward3/Backward3/Stop3`, той самий, яким
> користується `StateGoToHome`/`StateBackwardMoving`) шле **одну** команду
> регістру 7 з `MotorID = 3` (= `1 | 2`, обидва біти разом), а не дві окремі
> команди з ID=1 і ID=2. Схоже, плата апаратно підтримує синхронний рух пари
> моторів через комбінований MotorID в одному кадрі — саме так гарантується,
> що вони завжди рухаються разом. Підтверджено фізично: заводський застосунок
> (кнопки Forward/Backward/Backward to home) рухає візок саме так; наші
> скрипти з двома окремими командами (ID=1, ID=2) руху не дали.

### Послідовність запуску

Пряма спроба (`ResetErrorMask` → `SetMotorRequirementsMask` → `SetMotorState`
→одразу→ `SetMotorSettings`) на практиці **не рухала** візок, хоча плата
приймала кадри й реле клацало.

> **Виправлено 2026-07-25:** попередня версія цього розділу помилково
> посилалась на `FeedingSystem.BwBoardDE_DoWork` — це клас **автоматичного
> маршруту**, окремий і набагато складніший, з іншою послідовністю. Кнопка
> **Start**, яка стосується ручної панелі (`DoForward`/`DoBackward`/
> `DoBackwardToHome`, ті самі, що ми копіюємо для калібрування), насправді
> запускає `ManualControlViewModel.BwBoardDE_DoWork` — і порядок там інший:
> **силове реле вмикається ПЕРШИМ, `ResetErrorMask` — вже ПІСЛЯ нього**,
> і пауза після імпульсу лише 1с, не 3с:

```
KeepAlive()
SetImpulseOutState(true, false)     // рег.10, вихід1 = ON
Sleep(1000 мс)
SetMotorRelay(true)                 // рег.17 — силове реле, ДО reset
SetImpulseOutState(false, false)    // рег.10, вихід1 = OFF знову
Sleep(1000 мс)                      // не 3000!
ResetErrorMask(усі 4 прапорці)
SetMotorRequirementsMask(маска задіяних моторів)
KeepAlive()
ResetErrorMask(усі 4 прапорці)
```

Ключове: **секундний імпульс на виході 1 (регістр 10) перед вмиканням
силового реле**. Призначення імпульсу не з'ясоване (пробудження драйвера
моторів? розблокування гальма?). Далі в реальному коді йде фоновий цикл
(кожні ~100мс): `KeepAlive()` + `GetInductiveOpticalSensorState()`
(рег.11) — це і є джерело правильної частоти опитування датчиків у наших
скриптах. Виправлено в `go_home.js`, перевірка на залізі — далі.

### Зворотний зв'язок (регістр 5)

`MotorID, Status, CurrentPower, CurrentPowerDelay, CurrentSpeed, CurrentRampUp,
CurrentRampDown, CurrentDirection`

---

## 7. Датчики

**Регістр 11** — індуктивні / оптичні. **Виправлено після першого реального
тесту на залізі** (`go_home.js`, 2026-07-25): відповідь плати — **РІВНО 2
байти**, а не 7, як тут стояло раніше (припущення "5 булевих байт + uint16
лічильник" ніде не звірялось, доки не побачили живий кадр `data=[12,16]`).
Реальний формат підтверджено з IL (`BoardProtocolProvider` — код, що будує
`MessageInductiveOpticalSensor` з сирих байтів):

```
d[0]: біт-поле — 0x01=IsInHomeMarker, 0x02=IsInPosMarker, 0x04=IsFullSensorOpen,
      0x08=IsEmptySensorOpen, 0x10=IsFrontSensorOpen
d[1]: PositionMarkerCounter — uint8 (НЕ uint16!), з переповненням на 255
```

Через це старий `decodeSensors` (гейтований на `data.length >= 7`) ніколи не
спрацьовував на живій платі — виявлення домашнього маркера в `go_home.js`
мовчки не працювало жодного разу, поки не полагодили формат.

**Регістр 15** — дискретні входи. Так само виправлено: відповідь — **РІВНО 1
байт**, бітове поле (не 6 окремих байтів, як тут стояло раніше), підтверджено
з IL (`MessageDigitalInputs`):

```
d[0]: біт-поле — 0x01=StopButton, 0x02=ResetButton, 0x04=EmergencyStopButton,
      0x08=WeightingInput, 0x10=SpareInput1, 0x20=SpareInput2
```

> **`StopButton` — ІНВЕРТОВАНИЙ біт.** Підтверджено з IL, єдине місце в
> застосунку, де рег.15 реально гейтує рух (`FeedingSystem`, обробник
> `MessageDigitalInputs`):
> ```
> EmergencystopState = (StopButton == 1) ? false : true
> ```
> Тобто `StopButton=1` означає "контур Stop/E-Stop цілий, руху нічого не
> заважає" (типова NC-схема безпеки: обрив/натиск розмикає лінію →
> `StopButton=0` → `EmergencystopState=true`). `EmergencyStopButton` і
> `ResetButton` біти в цій формулі НЕ використовуються — жодне місце в
> застосунку не гейтує рух на них напряму (перевірено, тільки виводяться на
> екран у діагностичних панелях). Перший робочий скрипт (`go_home.js`)
> спершу мав протилежну, неперевірену умову (`stopButton || emergencyStop`
> = блокувати) — вона хибно зупиняла старт при цілком справному контурі
> (`StopButton=1`), поки не звірили з `FeedingSystem` в IL. Виправлено.

**Регістр 14** — аналогові: `VoltageInput1..3, VoltageCharge, CurrentInput1..2`

**Регістр 4** — помилки: `InternalErrorMask, MotorLostCommunMask,
MotorProtectionTriggeringMask` (usint16 маски, побітову розшифровку **не з'ясовано**)

---

## 8. Бездротові стрілки

### Фізика

Кожна стрілка — **окрема коробочка з антеною**, власною адресою, реле
й датчиком положення. Від коробочки йде провід до свого сервопривода.
Позиція в коді — `bool`, тобто **рівно два положення**.

У поточній установці: **2 коробочки**.

### Словник команд (`EWirelessState`)

| Код | Команда | Призначення |
|---:|---|---|
| 1 | `CheckIn` | зареєструватися, зайняти зону |
| 2 | `SetWay` | перевести стрілку |
| 3 | `CheckOut` | звільнити зону |
| 4 | `Reset` | скид вузла |
| 5 | `SetSwAmount` | задати кількість стрілок |
| 6 | `GetCardId` | прочитати ID вузла |
| 7 | `Ping` | перевірка зв'язку (нічого не рухає) |
| 8 | `SendDirection` | передати напрямок |

### Два способи адресації

**А. Пряма команда — регістр 28.** Два байти: `[адреса][команда]`.
Вузол адресується **явно**. Використовується для `RailSwitcher` і `StandaloneSwitch`.

У `StateSwitchRail` виклик має вигляд `SetStandAlone(node.Address, IsB ? 2 : 1)` —
тобто для окремої коробочки **команди 1 і 2 означають два положення сервопривода**.

**Б. Таблиця зони — регістр 19.** Адреси в пакеті **немає** — стрілка визначається
**порядковим номером**. Прив'язка «номер → фізичний вузол» живе в конфігурації
плати й вузлів.

Пакет — 11 байт + 1 байт timeout:

```
[0]   команда (1=CheckIn, 2=SetWay, 3=CheckOut)
[1]   стрілки 1 і 2
[2]   стрілки 3 і 4
...
[10]  стрілки 19 і 20
[11]  timeout
```

Бітова розкладка кожного байта — по 3 біти на стрілку:

| Маска | Значення |
|---|---|
| `0x01` | стрілка N: позиція |
| `0x02` | стрілка N: оновити |
| `0x04` | стрілка N: блок |
| `0x10` | стрілка N+1: позиція |
| `0x20` | стрілка N+1: оновити |
| `0x40` | стрілка N+1: блок |

Біт **«оновити»** керує тим, чи взагалі чіпати цю стрілку. Якщо 0 — вузол лишає
її як є.

Є три варіанти таблиці: `Value` (вперед), `Value_back` (назад), `Value_block`.

### Handshake

Стан-машина `RTPerformer.States`. Послідовність біля кожної зони стрілок:

```
візок у зоні -> DriveStop() -> CheckIn -> SetWay/SetBlock -> CheckOut -> рух далі
```

**Візок фізично зупиняється** (`FeedingSystem.DriveStop()`) і не рушить, доки вузол
не підтвердить положення. 5 спроб, потім `AlarmHandling`.

### Підтвердження

**Регістр 22** (`WirelessACK`): `Status, NodeId, RelayStatus, SwitchPosition`
`SwitchPosition` — фактичне положення серво, а не факт прийому команди.

**Регістр 26**: `State, LastCommand`
**Регістр 29**: `LastAddr, State`

---

## 9. Перевірені кадри

Згенеровані реалізацією, звіреною з DLL. Придатні для порівняння зі сніфером.

**Мотор (регістр 7):**
```
СТОП мотор 0          : 7e 00 47 20 09 00 00 00 00 00 00 00 00 00 68 42 7e
РУХ мотор 0 sp=40 d=0 : 7e 00 47 20 09 00 01 01 3c 00 28 00 00 00 ad c0 7e
РУХ мотор 0 sp=40 d=1 : 7e 00 47 20 09 00 01 01 3c 00 28 00 00 01 24 d1 7e
```

**Читання:**
```
статус мотора (р.5)   : 7e 00 45 00 00 ea 3c 7e
датчики       (р.11)  : 7e 00 4b 00 00 f1 2c 7e
кнопки/E-Stop (р.15)  : 7e 00 4f 00 00 90 4f 7e
keepalive     (р.1)   : 7e 00 41 00 00 8b 5f 7e
```

**Стрілки, пряма команда (регістр 28):**
```
адреса 1, положення A : 7e 00 5c 20 02 01 01 98 38 7e
адреса 1, положення B : 7e 00 5c 20 02 01 02 03 0a 7e
адреса 2, положення A : 7e 00 5c 20 02 02 01 f0 12 7e
адреса 2, положення B : 7e 00 5c 20 02 02 02 6b 20 7e
Ping адреси 1         : 7e 00 5c 20 02 01 07 ae 5d 7e
Ping адреси 2         : 7e 00 5c 20 02 02 07 c6 77 7e
GetCardId адреси 1    : 7e 00 5c 20 02 01 06 27 4c 7e
```

**Стрілки, таблиця (регістр 19)**, серво1 → поз.1, серво2 → поз.0, обидва оновити
(байт `[1]` = `0x23`):
```
CheckIn  : 7e 00 53 20 0c 01 23 00 00 00 00 00 00 00 00 00 14 d4 e0 7e
SetWay   : 7e 00 53 20 0c 02 23 00 00 00 00 00 00 00 00 00 14 3a 67 7e
CheckOut : 7e 00 53 20 0c 03 23 00 00 00 00 00 00 00 00 00 14 6f e2 7e
```

> Адреси `1` і `2` — **припущення**. Реальні треба з'ясувати (див. розділ 12).

---

## 10. Код у проєкті

```
powernet.js        реалізація протоколу на JS (CRC, кадр, стаффінг, парсер)
verify.js          тест: звіряє кадри з еталоном, перевіряє round-trip і стаффінг
server.js          керуюче ядро: serial + keepalive + мертвий вимикач + WebSocket
powernet_motor.py  та сама реалізація на Python (для швидких експериментів)
```

Запуск перевірки:

```bash
node verify.js          # має вивести "ВСЕ ЗБІГАЄТЬСЯ"
python3 powernet_motor.py
```

Запуск ядра:

```bash
npm install serialport ws
PORT_NAME=/dev/ttyUSB0 node server.js     # Windows: PORT_NAME=COM3
```

### Архітектурний принцип

Керуюче ядро **володіє портом і не залежить від UI**. Якщо браузер завис, закрився
чи зникла мережа — мотор зупиняється сам:

- keepalive кожні 300 мс
- мертвий вимикач: немає сигналу від панелі 1.5 с → `Status = 0`
- втрата зв'язку з платою → стоп
- `StopButton==0` у регістрі 15 (розімкнений контур Stop/E-Stop) → стоп і
  блокування пуску. **Біт інвертований** — `1` означає "контур цілий", див. §7
- `SIGINT` / `SIGTERM` / необроблена помилка → відправка стопу перед виходом

Ця логіка **не має переїжджати в UI**. Браузерна вкладка у фоні throttlиться
до одного таймера на хвилину — керувати мотором звідти не можна.

---

## 11. Безпека

Це не абстрактний протокол — код рухає багатотонну машину в приміщенні з тваринами.

- **Коліс, які можна підняти, немає.** Візок — підвісний, висить на роликах
  на монорейці; Drive1+Drive2 крутять самі ролики. Немає способу "відчепити"
  привід від руху — щойно мотори реально запрацюють, візок фізично поїде по
  рейці. Ізольованого "безпечного" тесту на місці не існує
- `Speed` 10–20 виявився **нижче порогу зрушення** (мотор не крутиться) —
  реально працює `Speed` ≈ 100+ (див. формулу вище). Це означає: щойно команда
  реально спрацює, візок одразу поїде з помітною швидкістю, без плавного
  розгону "на пробу". Перший успішний тест — **найкоротший можливий пробіг**
  (секунда-дві), одразу готовий Ctrl+C/фізичний E-Stop, і тільки після цього
  довші прогони
- Перед пуском читати регістр 15 і перевіряти `StopButton` — **`StopButton==1`
  означає "все ОК", `==0` означає реальний Stop/E-Stop** (інвертовано, див.
  §7 і §12 п.0); `EmergencyStopButton` окремо ніде в застосунку не гейтує рух
- Фізичний аварійний стоп під рукою
- `Direction` 0/1 звірити на місці на малій швидкості
- Не тримати заводський застосунок відкритим одночасно зі своїм софтом —
  два майстри на одній лінії дадуть конфлікт кадрів
- Стрілки: пряма команда через регістр 28 **не робить CheckIn і не перевіряє
  блокування**. Смикати тільки коли візок стоїть
- Регістр 2 (`ForceToBootloaderMode`) — не чіпати взагалі

---

## 12. Що ще не з'ясовано

Впорядковано за практичною користю:

0. **Мотор не рухається — досі не підтверджено на залізі, дві причини вже
   виправлено, третя (найімовірніша) чекає перевірки.** Хронологія
   (2026-07-25):
   - Прогін 1: boot-послідовність пройшла без помилок, реле рег.17
     увімкнулось, рег.7 приймався й ACK-ився щоразу, але рег.6 весь час
     показував `status=0 speed=0` — мотор фізично не стартував.
   - Прогін 2: додали перевірку рег.15 — вона показала
     `stop=true eStop=true reset=true` і скрипт сам заблокував старт.
     Користувач зауважив: у заводській панелі досить просто натиснути
     "Старт" — і все їде, за тих самих фізичних умов. Звірка з
     `FeedingSystem` в IL (єдине місце, де рег.15 реально гейтує рух)
     показала: `EmergencystopState = (StopButton==1) ? false : true` —
     **`StopButton` інвертований**, `1` означає "контур цілий". Виправлено
     в `go_home.js`/`server.js` (§7/§11).
   - Прогін 3 (з виправленим інвертованим StopButton): гейт більше не
     блокував старт, скрипт дійшов до циклу руху — **але візок фізично не
     зрушив** (підтверджено користувачем), рег.6 і далі `status=0`.
   - Звідси: питання не в рег.15. Перевірили `ErrorBoardHandle` в
     `FeedingSystem` (реальний обробник помилок для автоматичного руху) —
     біт `0x100` у рег.4 (`InternalErrorMask`) не входить у жоден із
     перевірених там бітів (`0x1,0x2,0x4,0x8,0x10,0x20,0x40`), тобто й
     автоматична логіка його ігнорує — малоймовірно, що він щось блокує.
   - Знайдено ймовірну справжню причину: **`CLAUDE.md` раніше описував
     boot-послідовність із класу `FeedingSystem.BwBoardDE_DoWork`**
     (автоматичний маршрут) **замість `ManualControlViewModel.BwBoardDE_DoWork`**
     (та сама панель, чиї `DoForward`/`DoBackward`/`DoBackwardToHome` ми
     копіюємо для калібрування). Реальний порядок інший: `SetMotorRelay(true)`
     вмикається ПЕРШИМ, `ResetErrorMask` — вже ПІСЛЯ нього, і пауза після
     імпульсу лише 1с, не 3с (повна послідовність — §6 "Послідовність
     запуску"). **Виправлено в `go_home.js` (2026-07-25), ще не перевірено
     на залізі.**
   - `server.js` НЕ має автоматизованої boot-послідовності взагалі (оператор
     сам тумблерами вмикає імпульс/реле через UI) — якщо виправлення
     порядку в `go_home.js` підтвердиться, варто додати таку саму
     послідовність і туди як окрему дію "Старт".
   **Наступний крок:** запустити виправлений `go_home.js` на залізі.
1. **Реальні адреси коробочок-стрілок.** Способи: подивитися поле `Adress` у вузлі
   в редакторі маршруту; послати `Ping` (кмд 7) по адресах 1…N і дивитися регістр 29
   (`LastAddr`); або `GetCardId` (кмд 6).
2. ~~Відповідність `MotorID` фізичним приводам~~ — **з'ясовано**, див. таблицю
   калібрування в §6 (звірено `Settings.xml` + побудова маски рег.25 в IL).
3. **Побітова розшифровка масок помилок** регістра 4 — частково з'ясовано:
   `strBoardError` в `ManualControlViewModel` (IL) розпізнає лише
   `0x01=Error_text1, 0x02=Error_text2, 0x04=Error_text3(+по мотору з
   MotorLostCommunMask), 0x08=Error_text4(+по мотору з
   MotorProtectionTriggeringMask), 0x10=Error_text5, 0x40=Error_text6` —
   самі тексти (`Properties.Strings.Error_textN`) не витягнуті (лежать у
   .resources, не в чистому IL). **Біт 0x100, який реально спостерігався на
   живій платі, не входить у цей список** — досі не з'ясовано, що він означає.
4. **Семантика `Block`.** За назвами (`BlocksOnWay`, `IsBlockWay`, `SetUnBlock`)
   схоже на взаємне блокування ділянок колії, але прошивку вузла не бачили.
5. **Призначення байта `ev`** у команді мотора (байт 1, регістр 7).
6. **Формат файлу маршруту** — там лежать адреси вузлів, `IsB`, таймаути.
   Якщо розібрати, адреси можна прочитати з конфігу без пінгування наосліп.
7. **Точний зміст `StateSwitchingZone`** (байт 0 таблиці до перезапису командою).

---

## 13. Як продовжити в терміналі

```bash
mkdir powernet && cd powernet
# покласти сюди CLAUDE.md і файли коду
claude
```

Claude Code підхопить `CLAUDE.md` автоматично як контекст.

Корисно покласти поруч і `PowerNetDriver.il` — тоді можна просити уточнити
будь-яку деталь протоколу прямо з дизасембльованого коду:

```bash
ikdasm app/PowerNetDriver.dll > PowerNetDriver.il
```

Приклади задач, які логічно робити далі:

- дописати в `server.js` читання й декодування регістра 4 (маски помилок)
- зробити CLI для пінгу вузлів і пошуку адрес стрілок
- зібрати UI панелі (HTML + WebSocket до `server.js`)
- написати сніфер, що слухає лінію паралельно й декодує кадри заводського
  застосунку — найшвидший спосіб закрити всі відкриті питання розділу 12
